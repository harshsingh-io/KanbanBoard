## 2024-05-23 - [Form Accessibility Patterns]
**Learning:** Adding `imeOptions="actionDone"` with an `OnEditorActionListener` is a critical micro-interaction for forms, significantly reducing friction for returning users.
**Action:** Always verify form inputs have proper `imeOptions` and `autofillHints` during UX reviews.
